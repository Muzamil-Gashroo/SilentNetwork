{
  "name": "silent-network",
  "version": "1.0.0",
  "description": "Resilient Offline Communication Network",
  "scripts": {
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:network": "concurrently \"npm run dev:server\" \"cd client && npm run dev:host\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
